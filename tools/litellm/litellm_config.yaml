# Example LiteLLM config for ForgetPhys debate (NO real keys).
# Docs: https://docs.litellm.ai/docs/proxy/configs
# App calls alias `forgetphys-debate` via Vercel AI SDK → LITELLM_BASE_URL.

model_list:
  # --- OpenAI example (commented; enable + set OPENAI_API_KEY in env) ---
  # - model_name: forgetphys-debate
  #   litellm_params:
  #     model: openai/gpt-4o-mini
  #     api_key: os.environ/OPENAI_API_KEY

  # --- Ollama local example (default placeholder for offline/dev) ---
  - model_name: forgetphys-debate
    litellm_params:
      model: ollama/llama3.2
      api_base: http://localhost:11434

  # --- DeepSeek example (commented) ---
  # - model_name: forgetphys-debate
  #   litellm_params:
  #     model: deepseek/deepseek-chat
  #     api_key: os.environ/DEEPSEEK_API_KEY

# litellm_settings:
#   drop_params: true
